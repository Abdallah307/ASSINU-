import React from "react";
import { View, Text } from "react-native";
import { Tab } from "react-native-elements";

const SharingCenterRequests = (props) => {
   
  return (
    <View style={{ flex: 1 }}>
      
    </View>
  );
};

export default SharingCenterRequests;
